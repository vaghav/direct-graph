package com.collibra.message.util;

/**
 * Represents the possible commands received from the client.
 */
public enum Command {
    INVALID(), HI, BYE, ADD_NODE, REMOVE_NODE, ADD_EDGE, REMOVE_EDGE;
}
